services:
  - type: web
    name: bluestream-cyber-yt
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    plan: free
    envVars:
      - key: PORT
        value: 10000
